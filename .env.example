# Example Environment variables for Docker Compose
# Copy this file to .env and fill in your specific values.
# .env file should NOT be committed to Git.

# Backend Configuration (Service name 'backend-postgres' is hardcoded in docker-compose.yml)
BACKEND_INTERNAL_PORT=8001

# Frontend Configuration / CORS
# Define allowed origins for CORS, comma-separated.
# IMPORTANT: Set this in your .env file based on your environment!
# Example for local dev: ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
# Example for production: ALLOWED_ORIGINS="https://yourdomain.com"
ALLOWED_ORIGINS=""

# Optional: Define a project name for Docker Compose to namespace containers/networks
COMPOSE_PROJECT_NAME=userinfo_dev